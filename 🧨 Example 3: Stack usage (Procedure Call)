.text
.globl main

main:
    li $a0, 5
    jal square
    move $t0, $v0

    li $v0, 10
    syscall

square:
    addi $sp, $sp, -4
    sw $ra, 0($sp)

    mul $v0, $a0, $a0

    lw $ra, 0($sp)
    addi $sp, $sp, 4
    jr $ra
